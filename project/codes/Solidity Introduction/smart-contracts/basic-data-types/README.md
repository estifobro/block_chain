Basic Data Types Exercises

Place your contract implementation for each exercise inside its respective `Contract.sol` file.

Exercises:

1. Booleans — `booleans/Contract.sol`
2. Unsigned Integers — `unsigned-integers/Contract.sol`
3. Signed Integers — `signed-integers/Contract.sol`
4. String Literals — `string-literals/Contract.sol`
5. Enum — `enum/Contract.sol`
